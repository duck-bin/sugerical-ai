# ─────────────────────────────────────────────────────────────────────────────
# Surgical CVS AI — pinned Python dependencies   (Python >= 3.11)
#
# Install torch / torchvision FIRST with CUDA 12.1 wheels:
#   pip install torch==2.4.1 torchvision==0.19.1 \
#       --index-url https://download.pytorch.org/whl/cu121
# Then:
#   pip install -r requirements.txt
#
# SAM2 is loaded through transformers (Sam2Model) — no separate install.
# ─────────────────────────────────────────────────────────────────────────────

# --- Deep learning core (install via cu121 index, see header) ---
torch==2.4.1
torchvision==0.19.1

# --- Parameter-efficient fine-tuning ---
peft>=0.13.0

# --- Models / backbones ---
transformers>=5.8,<6               # SAM2 (Sam2Model) — verified with 5.8.1
timm>=1.0.9                        # ViT-Small + EfficientNet-B4 backbones
segmentation-models-pytorch>=0.3.4 # U-Net baseline only

# --- Data loading / augmentation ---
datasets>=3.0.0                    # HuggingFace loader for CholecSeg8k
albumentations>=2.0,<3            # 2.x API used by src/data/transforms.py
opencv-python-headless>=4.10.0
pycocotools>=2.0.8
pandas>=2.2.0
numpy>=1.26,<2.1                   # TODO: verify upper bound vs SAM2 / opencv

# --- Training framework / config / logging ---
pytorch-lightning>=2.4.0
hydra-core>=1.3.2
wandb>=0.18.0

# --- Evaluation / science ---
scikit-learn>=1.5.0
matplotlib>=3.9.0

# --- Demo ---
gradio>=4.44.0

# --- Tests ---
pytest>=8.3.0
pytest-cov>=5.0.0

# --- SAM2 ---
# SAM2 is loaded via transformers (transformers.Sam2Model) — no separate
# install needed. This avoids the native facebookresearch/sam2 package, which
# pins torch>=2.5.1 and pulls hydra-core. See src/models/sam2_lora.py.
